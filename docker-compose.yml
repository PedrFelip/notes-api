services:
  db:
    image: postgres:latest
    container_name: notes_db
    env_file:
      - .env
    ports:
      - 5432:5432

    volumes:
      - db_data:/var/lib/postgresql/data

    networks:
      - notes_network

volumes:
  db_data:
    driver: local

networks:
  notes_network:
    driver: bridge